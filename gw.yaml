apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: demoweb-gateway
  namespace: test 
spec:
  selector:
    app: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "demoweb.arecord.us"
